{
  "name": "@quizzi/api",
  "version": "1.0.0",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "start": "bun src/index.ts",
    "build": "tsc --noEmit",
    "type-check": "tsc --noEmit",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "db:seed": "bun prisma/seed-questions.ts",
    "clean": "rm -rf dist node_modules"
  },
  "dependencies": {
    "@quizzi/types": "workspace:*",
    "@quizzi/utils": "workspace:*",
    "@prisma/client": "^5.9.1",
    "hono": "^4.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@quizzi/config": "workspace:*",
    "@types/bun": "^1.0.0",
    "prisma": "^5.9.1",
    "typescript": "^5.3.3"
  },
  "private": true
}